% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-spec.R
\name{validate_model_spec}
\alias{validate_model_spec}
\title{Validate a stressPLS model specification}
\usage{
validate_model_spec(model, data = NULL)
}
\arguments{
\item{model}{A \code{stresspls_model_spec} object.}

\item{data}{Optional data frame used to check required indicators.}
}
\value{
The input \code{model}, invisibly, when validation succeeds.
}
\description{
\code{validate_model_spec()} checks the internal consistency of a
\code{stresspls_model_spec} and, optionally, whether all manifest indicators are
present in a supplied data frame.
}
\examples{
image <- specify_construct("Image", c("img1", "img2"))
satisfaction <- specify_construct("Satisfaction", c("sat1", "sat2"))
paths <- specify_paths(from = "Image", to = "Satisfaction")
model <- specify_model(list(image, satisfaction), paths = paths)
validate_model_spec(model)

dat <- data.frame(img1 = 1:3, img2 = 2:4, sat1 = 3:5, sat2 = 4:6)
validate_model_spec(model, data = dat)
}
