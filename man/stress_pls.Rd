% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress-pls.R
\name{stress_pls}
\alias{stress_pls}
\title{Run a stressPLS analysis}
\usage{
stress_pls(data, model, grid = NULL, backend = NULL, seed = NULL)
}
\arguments{
\item{data}{A data frame containing the analysis data.}

\item{model}{A list describing the model specification.}

\item{grid}{Optional \code{stresspls_grid}. If omitted, an empty base grid is used.}

\item{backend}{Optional function with arguments \code{data}, \code{model}, \code{scenario},
and \code{seed}. It must return a data frame.}

\item{seed}{Optional random seed passed to backend calls.}
}
\value{
A \code{stresspls_result} object.
}
\description{
\code{stress_pls()} combines data, a model specification, a perturbation grid, and
an optional estimation backend. The scaffold intentionally does not implement
a PLS-SEM estimator. If \code{backend = NULL}, scenarios are returned with status
\code{"not_estimated"} rather than fabricated statistical results.
}
\examples{
dat <- data.frame(x1 = 1:4, x2 = 2:5, y = 3:6)
model <- list(indicators = c("x1", "x2"))
grid <- stress_indicators(dat, model)
stress_pls(dat, model, grid = grid)
}
