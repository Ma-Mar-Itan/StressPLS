% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{fit_perturbation_grid}
\alias{fit_perturbation_grid}
\title{Fit all valid scenarios in a perturbation grid}
\usage{
fit_perturbation_grid(
  model,
  data,
  grid,
  backend,
  continue_on_error = TRUE,
  fit_baseline = TRUE,
  ...
)
}
\arguments{
\item{model}{A \code{stresspls_model_spec}.}

\item{data}{Data frame containing required indicators.}

\item{grid}{A \code{stresspls_perturbation_grid}.}

\item{backend}{Backend function or \code{stresspls_backend}.}

\item{continue_on_error}{If \code{TRUE}, scenario-level backend errors are stored
as failed fit diagnostics. If \code{FALSE}, the first error is raised.}

\item{fit_baseline}{If \code{TRUE}, store a baseline fit on the returned object.}

\item{...}{Extra arguments passed to the backend.}
}
\value{
A \code{stresspls_fit_grid} object.
}
\description{
Fit all valid scenarios in a perturbation grid
}
\examples{
image <- specify_construct("Image", c("img1", "img2", "img3"))
model <- specify_model(list(image))
dat <- data.frame(img1 = 1:4, img2 = 2:5, img3 = 3:6)
grid <- leave_one_indicator_out(model)
toy_backend <- function(model, data, scenario = NULL, ...) {
  list(weights = data.frame(construct = "Image", indicator = "img1",
                            estimate = mean(data$img1)))
}
fit_perturbation_grid(model, dat, grid, toy_backend)
}
