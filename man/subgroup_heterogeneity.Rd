% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heterogeneity.R
\name{subgroup_heterogeneity}
\alias{subgroup_heterogeneity}
\title{Observed subgroup heterogeneity diagnostics}
\usage{
subgroup_heterogeneity(
  model,
  data,
  backend,
  group,
  min_n = 30,
  continue_on_error = TRUE,
  ...
)
}
\arguments{
\item{model}{A \code{stresspls_model_spec}.}

\item{data}{Data frame.}

\item{backend}{Backend function or object.}

\item{group}{Column name defining observed subgroups.}

\item{min_n}{Minimum subgroup sample size.}

\item{continue_on_error}{Store subgroup errors instead of stopping.}

\item{...}{Extra backend arguments.}
}
\value{
A \code{stresspls_heterogeneity} object.
}
\description{
Observed subgroup heterogeneity diagnostics
}
\examples{
image <- specify_construct("Image", c("img1", "img2"))
model <- specify_model(list(image))
dat <- data.frame(img1 = 1:8, img2 = 2:9, g = rep(c("a", "b"), each = 4))
toy <- function(model, data, scenario = NULL, ...) {
  list(weights = data.frame(construct = "Image", indicator = "img1",
                            estimate = mean(data$img1)))
}
subgroup_heterogeneity(model, dat, toy, group = "g", min_n = 2)
}
