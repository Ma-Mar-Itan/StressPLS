% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{as_backend}
\alias{as_backend}
\title{Create a stressPLS backend}
\usage{
as_backend(backend, name = NULL, description = NULL)
}
\arguments{
\item{backend}{Function or existing \code{stresspls_backend}.}

\item{name}{Optional backend name.}

\item{description}{Optional backend description.}
}
\value{
A \code{stresspls_backend} object.
}
\description{
\code{as_backend()} wraps an estimator function in a stable backend object. The
wrapped function must accept at least \code{model} and \code{data}; it should also
accept \code{scenario} or \code{...}.
}
\examples{
toy_backend <- function(model, data, scenario = NULL, ...) {
  list(paths = data.frame(from = "A", to = "B", estimate = 0.1))
}
as_backend(toy_backend, name = "toy")
}
