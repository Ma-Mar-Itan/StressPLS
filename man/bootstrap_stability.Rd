% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-stability.R
\name{bootstrap_stability}
\alias{bootstrap_stability}
\title{Bootstrap stability diagnostics}
\usage{
bootstrap_stability(
  model,
  data,
  backend,
  R = 500,
  seed = NULL,
  strata = NULL,
  continue_on_error = TRUE,
  ...
)
}
\arguments{
\item{model}{A \code{stresspls_model_spec}.}

\item{data}{Data frame.}

\item{backend}{Backend function or \code{stresspls_backend}.}

\item{R}{Number of bootstrap replications.}

\item{seed}{Optional random seed.}

\item{strata}{Optional column name for stratified resampling.}

\item{continue_on_error}{Store failed fits instead of stopping.}

\item{...}{Extra arguments passed to the backend.}
}
\value{
A \code{stresspls_bootstrap} object.
}
\description{
Bootstrap stability diagnostics
}
\examples{
image <- specify_construct("Image", c("img1", "img2"))
model <- specify_model(list(image))
dat <- data.frame(img1 = 1:6, img2 = 2:7)
toy <- function(model, data, scenario = NULL, ...) {
  list(weights = data.frame(construct = "Image", indicator = "img1",
                            estimate = mean(data$img1)))
}
bootstrap_stability(model, dat, toy, R = 2, seed = 1)
}
